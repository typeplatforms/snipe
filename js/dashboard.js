import { supabase, requireSession, touchActivity } from './supabase.js';
const session=await requireSession();if(!session)throw new Error('Not authenticated');await touchActivity();
const{data:profile,error}=await supabase.from('profiles').select('*').eq('id',session.user.id).single();if(error)throw error;
const initials=(profile.display_name||profile.username||'?').slice(0,1).toUpperCase();document.querySelector('#nav-avatar').textContent=initials;document.querySelector('#avatar').textContent=initials;document.querySelector('#display-name').textContent=profile.display_name||profile.username;document.querySelector('#handle').textContent='@'+profile.username;document.querySelector('#bio').textContent=profile.bio||'No bio yet.';document.querySelector('#views').textContent=profile.profile_views??0;document.querySelector('#joined').textContent=new Date(profile.created_at).toLocaleDateString(undefined,{month:'short',year:'numeric'});const{count}=await supabase.from('profile_badges').select('*',{count:'exact',head:true}).eq('profile_id',profile.id);document.querySelector('#badges').textContent=count??0;const link='profile.html?u='+encodeURIComponent(profile.username);document.querySelector('#public-link').href=link;document.querySelector('#public-nav').href=link;document.querySelector('#logout').addEventListener('click',async()=>{await supabase.auth.signOut();window.location.replace('index.html')});